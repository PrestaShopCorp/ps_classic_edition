export enum CallBackStatusEnum {
  CONTACT_IN_PROGRESS = "Contact in progress",
  CONTACTED_SUCCESSFULLY_STILL_OPENED = "Contacted successefully, Case still opened",
  CONTACTED_SUCCESSFULLY_CLOSED = "Contacted successefully, Case closed",
  PERMANENTLY_UNREACHABLE = "permanently unreachable",
  STILL_UNREACHABLE = "still unreachable",
  WRONG_NUMBER = "Wrong number",
}
